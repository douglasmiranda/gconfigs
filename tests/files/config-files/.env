[section]
CONFIG-1=config-1
config-2=2
 config-key-with-spaces =2
# COMMENTED-CONFIG=1
CONFIG-EMPTY-VALUE=
# Bellow a invalid line, key and value empty
=
config-value-with-spaces= config value with spaces
# It's important to keep this without a new line at the end of the file,
# to test that edge case as well
TEST-EMPTY-WITHOUT-NEWLINE=